Document Streak Prompt B in CLAUDE.md + fix E2E cleanup ordering

CLAUDE.md gets a new Sprint 20 (Prompt B) section covering safe-push,
the 20 outbound sites, the gap fill for marketing toggle "to false",
the inbound webhooks with shared-secret auth, and the post-dinner NTO
cleanup. Known issues call out the still-needed Streak automation
configuration and that no backfill has happened yet.

E2E script cleanup ordering fixed — applications must be deleted before
the member, and system_events.subject_member_id / actor_id need NULLing
before the member can be deleted (caught during the live run; orphan
member was cleaned up out-of-band).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
