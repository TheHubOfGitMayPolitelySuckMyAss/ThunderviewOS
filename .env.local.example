NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SITE_URL=
CRON_SECRET=
ANTHROPIC_API_KEY=
# Google OAuth client shared with DigiEric (mail-merge Gmail sending).
# Thunderview's callback URL must be whitelisted on the client in Google Cloud console.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Known Quantity monthly attendee CSV intake. The token in the path IS the
# credential — no bearer, no headers. Treat the whole URL as a secret.
KNOWN_QUANTITY_CSV_URL=
